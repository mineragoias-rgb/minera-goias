const $=id=>document.getElementById(id);let csrf='',user=null,data=null;const number=n=>I18N.num(n);const VIZ={blue:'#0f6fb0',teal:'#0d9488',amber:'#c2681b'};const niceStep=v=>{if(!(v>0))return 1;const p=Math.pow(10,Math.floor(Math.log10(v))),n=v/p;return (n<=1?1:n<=1.5?1.5:n<=2?2:n<=2.5?2.5:n<=3?3:n<=4?4:n<=5?5:n<=7.5?7.5:10)*p};const niceTop=v=>niceStep(Math.max(v,Number.MIN_VALUE)/4)*4;
const barPath=(x,y,w,h,r)=>{if(h<=0)return '';const rr=Math.min(r,w/2,h);return `M${x} ${y+h}V${y+rr}a${rr} ${rr} 0 0 1 ${rr} ${-rr}h${w-2*rr}a${rr} ${rr} 0 0 1 ${rr} ${rr}V${y+h}Z`};const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));async function api(path,method='GET',body){const r=await fetch('/api'+path,{method,headers:{'Content-Type':'application/json','X-CSRF-Token':csrf},...(body?{body:JSON.stringify(body)}:{})});if(r.status===401){location.replace('/login.html');throw Error(t('c.sessionEnded'))}const d=await r.json().catch(()=>null);if(!r.ok)throw Error(d&&typeof d.detail==='string'?d.detail:t('c.failed'));return d}function view(name){for(const key of ['overview','sources','admin','atlas','panorama','radar'])$(key+'-view').hidden=key!==name;document.querySelectorAll('[data-view]').forEach(b=>b.classList.toggle('active',b.dataset.view===name));if(name==='atlas'&&window.showAtlas)window.showAtlas();if(name==='panorama'&&window.showPanorama)window.showPanorama();if(name==='admin')loadUsers().catch(showError);if(name==='radar'&&window.showRadar)window.showRadar();if(name==='overview'&&window.showOverview)window.showOverview();if(name==='sources'&&!data)loadDashboard()}function showError(e){$('page-error').textContent=e.message}function renderSources(){if(!data)return;const q=$('source-search').value.toLowerCase();const rows=data.datasets.filter(d=>(d.path+' '+d.sheet_name).toLowerCase().includes(q));$('source-count').textContent=t('sv.count',{n:number(rows.length)});$('sources-body').innerHTML=rows.map(d=>{const warnings=Object.entries(d.warnings).filter(([,v])=>v&&v!==0);return `<tr><td class="path">${esc(d.path)}</td><td>${esc(d.sheet_name)}</td><td>${number(d.data_row_count)}</td><td>${warnings.length?`<details><summary>${esc(t('sv.alerts',{n:warnings.length}))}</summary>${warnings.map(([k,v])=>`<div>${esc(k)}: ${esc(typeof v==='object'?JSON.stringify(v):v)}</div>`).join('')}</details>`:`<span class="pill">${esc(t('sv.noAlert'))}</span>`}</td></tr>`}).join('')}async function loadDashboard(){$('page-error').textContent='';try{data=await PKG.get('/dashboard');renderSources()}catch(e){data=null;$('sources-body').textContent='';$('source-count').textContent='';showError(e)}}async function loadUsers(){const users=await api('/admin/users');$('users-body').innerHTML=users.map(u=>`<tr><td>${esc(u.name)}<br><span class="muted">${esc(u.username)}</span></td><td>${esc(t(u.role==='admin'?'role.admin':'role.user'))}</td><td>${esc(t(u.active?'ad.active':'ad.inactive'))}</td><td>${u.id===user.id?esc(t('ad.yourAccount')):`<button class="link-button" data-user-id="${u.id}" data-active="${u.active?'0':'1'}">${esc(t(u.active?'ad.disable':'ad.enable'))}</button>`}</td></tr>`).join('');$('users-body').querySelectorAll('button').forEach(b=>b.onclick=async()=>{b.disabled=true;try{await api('/admin/users/'+b.dataset.userId,'PATCH',{active:b.dataset.active==='1'});await loadUsers()}catch(e){showError(e);b.disabled=false}})}document.querySelectorAll('[data-view]').forEach(b=>b.onclick=()=>view(b.dataset.view));$('source-search').oninput=renderSources;$('change-password').onclick=()=>location.assign('/login.html#senha');$('logout').onclick=async()=>{try{await api('/auth/logout','POST',{});location.replace('/login.html')}catch(e){showError(e)}};$('create-user').onsubmit=async e=>{e.preventDefault();const b=e.target.querySelector('button');b.disabled=true;const m=$('admin-message');try{await api('/admin/users','POST',{name:$('new-name').value,username:$('new-username').value,password:$('initial-password').value,role:$('new-role').value});e.target.reset();m.className='success';m.textContent=t('ad.created');await loadUsers()}catch(err){m.className='error';m.textContent=err.message}finally{b.disabled=false}};(async()=>{try{const s=await api('/auth/me');user=s.user;csrf=s.csrf;if(user.must_change){location.replace('/login.html#senha');return}$('user-name').textContent=user.name;$('user-role').textContent=t(user.role==='admin'?'role.admin':'role.user');$('avatar').textContent=user.name.split(' ').map(x=>x[0]).slice(0,2).join('').toUpperCase();$('admin-nav').hidden=user.role!=='admin';view(location.hash==='#fontes'?'sources':location.hash==='#visao'?'overview':'atlas')}catch(e){showError(e)}finally{$('loading').hidden=true}})();
