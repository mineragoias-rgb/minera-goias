const $=id=>document.getElementById(id);let csrf='',user=null,data=null;const number=n=>Number(n).toLocaleString('pt-BR');const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));async function api(path,method='GET',body){const r=await fetch('/api'+path,{method,headers:{'Content-Type':'application/json','X-CSRF-Token':csrf},...(body?{body:JSON.stringify(body)}:{})});if(r.status===401){location.replace('/login.html');throw Error('Sessão encerrada.')}const d=await r.json();if(!r.ok)throw Error(typeof d.detail==='string'?d.detail:'Não foi possível concluir. Confira os campos.');return d}function view(name){for(const key of ['overview','sources','admin'])$(key+'-view').hidden=key!==name;document.querySelectorAll('[data-view]').forEach(b=>b.classList.toggle('active',b.dataset.view===name));if(name==='admin')loadUsers().catch(showError)}function showError(e){$('page-error').textContent=e.message}function bars(rows){const max=Math.max(...rows.map(x=>x.value),1);return rows.length?rows.map(x=>`<div class="bar-row"><span class="bar-label" title="${esc(x.label)}">${esc(x.label)}</span><div class="bar-track"><div class="bar-fill" style="width:${x.value/max*100}%"></div></div><span class="bar-value">${number(x.value)}</span></div>`).join(''):'<div class="empty">Nenhum registro para este recorte.</div>'}function months(rows){if(!rows.length)return '<div class="empty">Nenhum registro para este recorte.</div>';const max=Math.max(...rows.map(x=>x.value),1),w=600,h=250,left=46,bottom=212,step=530/rows.length;let svg=`<svg viewBox="0 0 ${w} ${h}" role="img" aria-label="Registros mensais da CCEE. Valores disponíveis na tabela abaixo.">`;for(let i=0;i<=4;i++){let y=bottom-i*45;svg+=`<line x1="46" x2="580" y1="${y}" y2="${y}" stroke="#e4edf2"/><text x="37" y="${y+4}" text-anchor="end" font-size="10" fill="#687e8f">${number(Math.round(max*i/4))}</text>`}rows.forEach((r,i)=>{const height=r.value/max*180,x=left+i*step+step*.2;svg+=`<rect x="${x}" y="${bottom-height}" width="${step*.6}" height="${height}" fill="#087eb7" rx="2"><title>${esc(r.label)}: ${number(r.value)} registros</title></rect><text x="${x+step*.3}" y="235" text-anchor="middle" font-size="10" fill="#687e8f">${esc(r.label.slice(4,6))}</text>`});return svg+'</svg>'}function renderSources(){if(!data)return;const q=$('source-search').value.toLowerCase();const rows=data.datasets.filter(d=>(d.path+' '+d.sheet_name).toLowerCase().includes(q));$('source-count').textContent=`${rows.length} abas/tabelas encontradas`;$('sources-body').innerHTML=rows.map(d=>{const warnings=Object.entries(d.warnings).filter(([,v])=>v&&v!==0);return `<tr><td class="path">${esc(d.path)}</td><td>${esc(d.sheet_name)}</td><td>${number(d.data_row_count)}</td><td>${warnings.length?`<details><summary>${warnings.length} tipos de alerta</summary>${warnings.map(([k,v])=>`<div>${esc(k)}: ${esc(typeof v==='object'?JSON.stringify(v):v)}</div>`).join('')}</details>`:'<span class="pill">Sem alerta do importador</span>'}</td></tr>`}).join('')}async function loadDashboard(){const b=$('filter-form').querySelector('button');b.disabled=true;$('page-error').textContent='';try{const d=await api('/dashboard?'+new URLSearchParams({year:$('year').value,activity:$('activity').value}));data=d;$('activity').innerHTML='<option value="">Todas as atividades</option>'+d.activities.map(x=>`<option value="${esc(x)}" ${x===d.activity?'selected':''}>${esc(x)}</option>`).join('');$('metric-records').textContent=number(d.records);$('metric-cities').textContent=number(d.cities);$('metric-months').textContent=number(d.months.length);$('metric-files').textContent=number(d.summary.files);$('data-note').textContent=d.note;$('months-chart').innerHTML=months(d.months);$('months-table').innerHTML='<table><thead><tr><th>Mês</th><th>Registros</th></tr></thead><tbody>'+d.months.map(x=>`<tr><td>${esc(x.label)}</td><td>${number(x.value)}</td></tr>`).join('')+'</tbody></table>';$('cities-chart').innerHTML=bars(d.municipalities);$('sectors-chart').innerHTML=bars(d.sectors);$('source-line').textContent='Fonte: '+d.source+' · Versão atual importada no banco. Uma linha representa um registro, não uma empresa única.';renderSources()}catch(e){data=null;for(const id of ['metric-records','metric-cities','metric-months','metric-files'])$(id).textContent='—';for(const id of ['months-chart','cities-chart','sectors-chart','months-table','sources-body'])$(id).textContent='';$('source-line').textContent='';showError(e)}finally{b.disabled=false}}async function loadUsers(){const users=await api('/admin/users');$('users-body').innerHTML=users.map(u=>`<tr><td>${esc(u.name)}<br><span class="muted">${esc(u.username)}</span></td><td>${u.role==='admin'?'Administrador':'Usuário'}</td><td>${u.active?'Ativo':'Desativado'}</td><td>${u.id===user.id?'Sua conta':`<button class="link-button" data-user-id="${u.id}" data-active="${u.active?'0':'1'}">${u.active?'Desativar':'Reativar'}</button>`}</td></tr>`).join('');$('users-body').querySelectorAll('button').forEach(b=>b.onclick=async()=>{b.disabled=true;try{await api('/admin/users/'+b.dataset.userId,'PATCH',{active:b.dataset.active==='1'});await loadUsers()}catch(e){showError(e);b.disabled=false}})}document.querySelectorAll('[data-view]').forEach(b=>b.onclick=()=>view(b.dataset.view));$('source-search').oninput=renderSources;$('year').onchange=()=>{$('activity').value=''};$('filter-form').onsubmit=e=>{e.preventDefault();loadDashboard()};$('change-password').onclick=()=>location.assign('/login.html#senha');$('logout').onclick=async()=>{try{await api('/auth/logout','POST',{});location.replace('/login.html')}catch(e){showError(e)}};$('create-user').onsubmit=async e=>{e.preventDefault();const b=e.target.querySelector('button');b.disabled=true;const m=$('admin-message');try{await api('/admin/users','POST',{name:$('new-name').value,username:$('new-username').value,password:$('initial-password').value,role:$('new-role').value});e.target.reset();m.className='success';m.textContent='Conta criada. Entregue a senha inicial diretamente à pessoa.';await loadUsers()}catch(err){m.className='error';m.textContent=err.message}finally{b.disabled=false}};$('export').onclick=()=>{if(!data){showError(Error('Carregue os dados antes de exportar.'));return}const safe=v=>'"'+String(v).replace(/^[=+@-]/,"'$&").replaceAll('"','""')+'"';const rows=[['fonte','ano','atividade','mes','registros'],...data.months.map(r=>[data.source,data.year,data.activity||'Todas',r.label,r.value])];const url=URL.createObjectURL(new Blob(['\ufeff'+rows.map(r=>r.map(safe).join(';')).join('\r\n')],{type:'text/csv;charset=utf-8'}));const a=document.createElement('a');a.href=url;a.download=`minera-goias-ccee-${data.year}-resumo.csv`;a.click();setTimeout(()=>URL.revokeObjectURL(url),1000)};(async()=>{try{const s=await api('/auth/me');user=s.user;csrf=s.csrf;if(user.must_change){location.replace('/login.html#senha');return}$('user-name').textContent=user.name;$('user-role').textContent=user.role==='admin'?'Administrador':'Usuário';$('avatar').textContent=user.name.split(' ').map(x=>x[0]).slice(0,2).join('').toUpperCase();$('admin-nav').hidden=user.role!=='admin';view(location.hash==='#fontes'?'sources':'overview');await loadDashboard()}catch(e){showError(e)}finally{$('loading').hidden=true}})();
